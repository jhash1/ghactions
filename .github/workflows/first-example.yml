name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
            - name: Welcome message
              run: echo "My first GH Actions Job"

            - name: List Files
              run: ls 

            - name: Read file
              run: cat README.md